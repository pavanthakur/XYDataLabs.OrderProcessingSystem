# Identity Portability

Phase 9 keeps identity portability as a local-only proof.

This proof may use a local Keycloak or equivalent local OIDC shape, but it must not replace or mutate the production Entra ID model.
